import pytest

from solution import lowest_common_ancestor


def test_placeholder():
    assert callable(lowest_common_ancestor)
    pytest.skip(
        "Placeholder tests for Lowest Common Ancestor of a BST will be added in M5."
    )
